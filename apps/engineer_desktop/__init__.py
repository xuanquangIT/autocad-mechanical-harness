"""Engineer approval desktop app (PySide6). Planned - see README.md in this package.

Until it exists, `cad-harness demo --commit` and the CLI `approve` path are the
approval surface. Approval must always be a human action outside the AI client.
"""
